/**
 * Main entry point for the Snake Game
 * Acts as a central entry point for initialization
 */
document.addEventListener('DOMContentLoaded', function() {
    // Don't create a new game instance here
    // The game is created by the load event listener in Game.js
    
    console.log('Snake Game DOM ready - waiting for window load to initialize game');
}); 